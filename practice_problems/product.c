#include <stdio.h>
#include<conio.h>

int main(){
    // Define two integers
    int num1 = 5;
    int num2 = 10;
    // Calculate the product of two numbers
    int product = num1 + num2;
    // Display the sum
    printf("%d", product);
    return 0;
}